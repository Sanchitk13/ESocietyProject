import { CustomServiceCategory } from './custom-service-category';

describe('CustomServiceCategory', () => {
  it('should create an instance', () => {
    expect(new CustomServiceCategory()).toBeTruthy();
  });
});
